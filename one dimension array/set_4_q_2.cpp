#include<iostream>
#include<vector>
#include<sstream>
#include<algorithm>
#include<stack>
using namespace std;

int main()
{
    int x,y;
    cout<<"Enter the size of jug x and jug y";
    cin>>x;
    cin>>y;
    int target;
    cout<<"target--";
    cin>>target;
    bool flag= false;
    vector<int>v(2,0);
    stack<int>st;
    if(x+y == target)
        flag=true;
    else if()

}