#include<iostream>
using namespace std;
int main()
{
    int a;
    if(3+2%5)
        cout<<"even this work"<<endl;
    if(a=10)
        cout<<"this will also work"<<endl;
    if(-5)
        cout<<"this too works";
}
/*if must contain non-zero numbers so any non zero value in if statement
 will considered as true(1) so the if will run it will be considered as 
 boolean expresion*/