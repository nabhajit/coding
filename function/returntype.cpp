#include<iostream>
#include<cmath>
using namespace std;
int sum(int x, int y)      //  different variable to get the values
{
    return x+y;
}
int main(){
   // cout<<sum(40,56);   //calling function
    int x,y;
  /*  cin>>x>>y;
    cout<<min(x,y);
    cout<<max(x,y); */ 
    cout<<sqrt(6);    //build in function
}