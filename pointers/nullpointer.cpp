#include<iostream>
using namespace std;
int main()
{
    int x=7;
    int* ptr=NULL;          //this is to declare a null pointer having a constant memory.
    int* ptr2=0;            //this is to declare a null pointer having a constant memory.
    int* ptr3='\0';         //this is to declare a null pointer having a constant memory.
    cout<<ptr3;
}